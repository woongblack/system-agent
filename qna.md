# 시스템 장애 분석 미션 (OOM, CPU Spike, Deadlock) - 실전 Q&A 및 회고 가이드

본 문서는 `agent-app-leak` 프로세스를 대상으로 진행한 시스템 장애 분석 미션의 핵심 평가 기준(Rubric)에 대한 예상 질문과 모범 답안을 정리한 학습용 자료입니다.

---

## 1. 트러블슈팅 도구 및 명령어 활용 (How-to)

**Q1. `monitor.sh`에서 메모리 증가 패턴을 추적하기 위해 사용한 명령어와 데이터 추출 방법을 구체적으로 설명해주세요.**
*   **답변:** `./monitor.sh` 스크립트를 백그라운드나 별도의 터미널 탭에서 실행하여 로그를 출력하게 한 뒤, `> monitor.log` 와 같이 파일로 리다이렉션하거나 실시간으로 터미널에서 관찰했습니다. 특히 `grep MEM` 이나 `tail -f monitor.log`를 활용해 수많은 로그 속에서 특정 시간대별로 물리 메모리 수치(예: `MEM: 10.5% -> 95.8%`)가 선형적으로 급상승하는 핵심 라인만 추출하여 분석의 증거로 삼았습니다.

**Q2. 프로세스의 CPU 사용률을 확인하기 위해 선택한 도구와 적용한 옵션의 의미를 구분하여 서술해주세요.**
*   **답변:** `top` 명령어를 주력 도구로 사용했습니다. 전체 시스템 목록 중 특정 프로세스를 찾기 위해 `top` 실행 후 `Shift + P`를 눌러 CPU 사용률 기준으로 정렬하거나, PID를 알아낸 후 `top -p [PID]` 옵션을 주어 해당 프로세스만 집중적으로 모니터링했습니다. 
*   **추가 심화 (병목 구분):** 또한, 단순히 해당 프로세스의 사용률뿐만 아니라 시스템 전체의 부하 성격을 파악하기 위해 `us(User Space)` 지표가 치솟는 것을 함께 확인하여, 이 장애가 I/O 대기(`wa`)가 아닌 어플리케이션 내부 로직의 무한 루프/과점유(CPU-Bound)로 인한 것임을 증명했습니다.

**Q3. 프로세스가 "살아있지만 멈춰있는 상태(Deadlock)"를 진단하기 위해 어떤 도구를 어떤 순서로 사용했는지, 판단 흐름을 논리적으로 제시해주세요.**
*   **답변:** 
    1.  **생존 확인:** 가장 먼저 `ps -ef | grep agent-app-leak` 명령어를 통해 프로세스가 강제 종료(kill) 당하지 않고 해당 PID가 백그라운드에 분명히 살아있음을 확인했습니다.
    2.  **자원 사용량 확인:** 이어서 `top -p [PID]` (또는 `ps -L`)를 실행하여 해당 프로세스의 CPU와 MEM 사용률 추이를 지켜봤습니다. 둘 다 변화 없이 0% 근처에서 완전히 정체된 것을 확인하여 "아무 연산도 안 하고 있다"는 것을 알았습니다.
    3.  **로그 확인 (원인 확정):** 마지막으로 `tail -f`를 통해 어플리케이션 자체 로그를 확인해보니, 아무런 출력 없이 멈춰 있었고 마지막 로그가 `WAITING... (Status: BLOCKED)` 상태로 교차되어 있는 것을 발견하여 완벽한 무응답(Deadlock) 상태임을 진단했습니다.

---

## 2. 시스템 보호 정책 및 CS 동작 원리 (Why)

**Q4. 메모리 누수 시 애플리케이션의 메모리 보호 정책이 해당 프로세스를 강제 종료(Self-Terminate) 시키는 이유를 설명해주세요.**
*   **답변:** 하나의 악성(결함이 있는) 프로세스가 물리 메모리를 끝까지 점유하게 방치하면, 결국 운영체제(OS) 전체의 가용 메모리가 고갈(Out of Memory)됩니다. 이렇게 되면 해당 프로세스뿐만 아니라 서버에서 돌고 있는 다른 핵심 서비스(SSH, 네트워크 데몬 등)까지 연쇄적으로 멈추는 시스템 전체 다운(Kernel Panic 등)으로 이어집니다. 이를 막기 위해 프로세스 내부의 방어 로직(MemoryGuard)이 시스템 파국이 오기 전 임계치(Limit)에서 스스로를 강제 종료(Fail-Fast)하여 전체 시스템을 보호하는 것입니다. (또는 OS 레벨의 OOM Killer가 이 역할을 수행합니다.)

**Q5. 로그에서 스레드 간 순환 의존 관계(Deadlock)를 어떻게 파악했는지 추적 과정을 설명해주세요.**
*   **답변:** 로그에 찍힌 타임스탬프와 메시지의 교차 패턴을 역추적했습니다. 
    *   `Thread-1`이 `Resource A`의 Lock을 Acquired(점유)한 상태라는 로그를 보았습니다. (점유 대기)
    *   동시에 `Thread-2`는 `Resource B`의 Lock을 Acquired(점유)한 상태였습니다.
    *   그 직후, `Thread-1`은 `Resource B`를 요구하며 Wait 상태에 빠지고, `Thread-2`는 `Resource A`를 요구하며 Wait 상태에 빠지는 로그가 연달아 출력되었습니다. 
    *   이를 통해 서로가 상대방이 가진 자원을 끝까지 놓지 않고 무한정 기다리는 데드락의 핵심 조건인 '순환 대기(Circular Wait)' 상황이 성립했음을 파악했습니다.

---

## 3. 실무 응용 및 아키텍처 (Advanced / 시니어 관점)

**Q6. 만약 이번 미션의 애플리케이션이 실제 운영 서버에서 동작하고 있었다면, 메모리 누수를 장애 발생 "전"에 탐지하기 위해 현재의 `monitor.sh`를 어떻게 개선하시겠습니까?**
*   **답변:** 현재의 쉘 스크립트는 수동적인 관찰 도구에 불과합니다. 이를 개선하기 위해 **자동화된 알람(Alert) 시스템**을 결합하겠습니다.
    1.  스크립트 내부에 임계치 판단 로직(예: 메모리 80% 초과)을 넣고, 조건 만족 시 슬랙(Slack) 웹훅이나 이메일을 호출(`curl`)하도록 경고 발송 기능을 추가하겠습니다.
    2.  나아가, 이 수치를 텍스트 로그로만 쌓지 않고 Prometheus 같은 시계열 데이터베이스로 전송하도록 하여, Grafana 같은 대시보드 도구에서 메모리 우상향(Leak) 기울기를 시각적으로 모니터링할 수 있는 체계를 구축하겠습니다.

**Q7. 겪어본 3가지 장애(OOM, CPU Spike, Deadlock) 중 실제 서비스 환경에서 "가장 치명적인 것"은 무엇이라 생각하며, 이를 근본적으로 예방할 방법은 무엇입니까?**
*   **답변:** **Deadlock(교착상태)이 단연코 가장 치명적입니다.**
    *   **이유:** OOM이나 CPU Spike는 어쨌든 임계치에 다다르면 프로세스가 터지거나 죽기(Crash) 때문에, 앞단의 로드밸런서(L4/L7)나 오케스트레이션 도구(Kubernetes)가 "이 서버 죽었네" 하고 인지하여 트래픽을 차단하고 컨테이너를 재시작(Auto-healing) 시켜버립니다. 하지만 **Deadlock은 프로세스(PID)가 살아있고 응답(Health Check 등)을 속일 수 있는 '좀비(Zombie)' 상태**가 되므로, 인프라가 장애를 인지하지 못한 채 사용자 트래픽을 계속 집어넣어 대규모 타임아웃과 2차 장애를 유발하기 때문입니다.
    *   **예방책:** 시스템을 설계할 때 다중 스레드가 자원에 접근하는 **순서를 일관되게 강제(Lock Ordering)**하여 '순환 대기' 조건 자체를 성립하지 못하게 하거나, 트랜잭션 타임아웃을 짧게 설정하여 오래 대기하면 스스로 락을 포기(Retry)하도록 회피 전략을 짜야 합니다.

**Q8. 만약 동일한 서버에서 OOM과 Deadlock이 동시에 발생했다면, 어떤 순서로 트러블슈팅을 진행하겠습니까?**
*   **답변:** **우선순위는 무조건 Deadlock 해결(프로세스 강제 종료 등)이 먼저입니다.**
    *   앞서 말했듯 데드락은 시스템을 기만하며 들어오는 요청(스레드)들을 무한정 대기열에 쌓아버립니다. 대기하는 스레드가 100개, 1000개 늘어날 때마다 각 스레드가 점유하는 스택(Stack) 메모리도 폭발적으로 늘어납니다. 즉, **데드락 자체가 전체 메모리를 고갈시켜 OOM을 가속화시키는 주범**일 확률이 매우 높습니다. 따라서 데드락 걸린 자원을 해제(Kill)시켜주면 대기하던 메모리가 반환되어 OOM 증상도 일시적으로 안정될 수 있습니다.

**Q9. 미션의 환경변수 조정(MEMORY_LIMIT, CPU_MAX_OCCUPY)은 임시 조치였습니다. 소스 코드를 직접 수정할 수 있다면 어떻게 고치겠습니까?**
*   **답변:** 
    *   **OOM 개선:** 프로그램 내의 전역 컬렉션(List, Map 등) 데이터가 무한히 쌓이지 않도록 주기적으로 오래된 데이터를 삭제하는 캐시 축출(LRU Cache 정책 등) 로직을 적용하거나, 쓰레기 데이터의 연결(참조)을 명시적으로 끊어 GC가 정상 수거할 수 있게 리팩토링하겠습니다.
    *   **CPU Spike 개선:** 무한 루프(`while(true)`) 안에 적절한 휴식(`Thread.sleep()`)을 주어 CPU 양보(Yield)를 유도하고, 과도한 덧셈/루프 연산은 비동기 작업(Message Queue 워커 등)으로 빼서 단일 스레드가 CPU를 독점하지 못하게 하겠습니다.

---

*(작성자 메모: 본 문서는 미션 과정에서 겪은 경험을 논리적이고 깊이 있는 시스템 엔지니어의 시각으로 답변할 수 있도록 훈련하기 위해 작성되었습니다.)*